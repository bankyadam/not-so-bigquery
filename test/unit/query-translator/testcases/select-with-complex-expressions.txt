--INPUT--
SELECT
  COALESCE(s.treatments.rti.id, CAST(program_id)),
  CAST(DATE_TRUNC(DATE(s.event_time, timezone), DAY)),
  COUNT(1)
FROM `ems-strategic-dashboard-test.email_sends.sends_123456789` s
--EXPECT--
SELECT COALESCE(s.treatments.rti.id, CAST(program_id)), CAST(DATE_TRUNC(DATE(s.event_time, timezone), DAY)), COUNT(1) FROM `ems-strategic-dashboard-test__email_sends.sends_123456789` AS s
