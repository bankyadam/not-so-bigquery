--INPUT--
SELECT
  COALESCE(s.treatments.rti.id, CAST(program_id)),
  CAST(DATE_TRUNC(DATE(s.event_time, timezone), DAY)),
  COUNT(1)
FROM `p.d.t` s
--EXPECT--
SELECT
	COALESCE(s.treatments.rti.id, CAST(program_id)),
	CAST(DATE_TRUNC(DATE(s.event_time, timezone), DAY)),
	COUNT(1)
FROM p__d.t AS s
