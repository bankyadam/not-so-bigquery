--INPUT--
SELECT
    INTERVAL 1 YEAR,
    INTERVAL 4 QUARTER,
    INTERVAL 12 MONTH,
    INTERVAL 6 WEEK,
    INTERVAL 42 DAY,
    INTERVAL -25 HOUR,
    INTERVAL 1500 MINUTE,
    INTERVAL 90000 SECOND,
    INTERVAL field WEEK

--EXPECT--
SELECT
    ('1'||'YEAR')::INTERVAL,
    ('4'||'QUARTER')::INTERVAL,
    ('12'||'MONTH')::INTERVAL,
    ('6'||'WEEK')::INTERVAL,
    ('42'||'DAY')::INTERVAL,
    ('-25'||'HOUR')::INTERVAL,
    ('1500'||'MINUTE')::INTERVAL,
    ('90000'||'SECOND')::INTERVAL,
    ("FIELD"||'WEEK')::INTERVAL
